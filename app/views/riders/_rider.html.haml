.profile
  %li
    %h2
      = gravatar_for rider.contact, size: 40
      = link_to rider.contact.name, rider
      %br
      = rider.location.borough.text
      |
      = rider.contact.phone
      |
      = rider.active? ? 'Active' : 'Inactive'