- provide(:title, @rider.account.contact.name)
.profile
  = render 'users/profile_header'
  .row
    .span10.offset1
      = render 'shifts/tables/teaser', shifts: @rider.shifts, caller: 'rider'
      = render @rider.account
      = render @rider.location
      = render @rider.rider_rating
      = render @rider.qualification_set
      = render @rider.skill_set
      = render @rider.equipment_set
  .center
    = link_to 'Edit Rider Profile', edit_rider_path(@rider)
    |
    = link_to 'See All Riders', riders_path
      