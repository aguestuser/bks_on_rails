- #input: submit_path (Str)

.row
  = form_tag submit_path, method: :get do
    .span7.offset1
      = text_field_tag :search, params[:search]
    .span2
      = submit_tag 'Search', name: nil, :class => 'btn btn-primary'
