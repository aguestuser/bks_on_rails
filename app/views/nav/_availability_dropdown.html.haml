%li#fat-menu.dropdown
  %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
    Availability
    %b.caret
  %ul.dropdown-menu
    %li= link_to "List", conflicts_path
    %li= link_to "Grid", '/grid/availability'
    - if can? :manage, Conflict
      %li.divider
      %li= link_to "Create Conflict", new_conflict_path
      %li= link_to "Request Conflicts", "/rider/request_conflicts_preview?base_path=#{current_path}"