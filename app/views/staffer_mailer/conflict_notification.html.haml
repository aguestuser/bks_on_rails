- week_end = @week_start + 6.days
= "hello world!"
%p
  = "#{@rider.name} has submitted the following conflicts for the week of #{@week_start.strftime("%-m/%-d")} - #{week_end.strftime("%-m/%-d")}:"
= render 'conflicts/review_list'
- if @notes
  %p
    %strong
      NOTES:
    = "#{@notes}"
