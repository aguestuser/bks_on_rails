- provide(:title, @manager.account.contact.name)
.profile
  .row
    .span8.offset2
      = gravatar_for @manager, size: 80
      %section
        %h1.name= @manager.account.contact.name
        %h2.title
          #{@manager.account.contact.title} | #{@manager.restaurant.name}
  .row
    .span8.offset2
      %h3 Contact Info
      %ul
        %li 
          %strong 
            Phone: 
          = @manager.account.contact.phone
        %li 
          %strong
            Email: 
          = @manager.account.contact.email
      .center
        = link_to 'Edit', edit_manager_path(@manager)
        | 
        = link_to "Delete", @manager, method: :delete, data: { confirm: "Are you sure? All information associated with #{@manager.name} will be permanently destroyed!" }