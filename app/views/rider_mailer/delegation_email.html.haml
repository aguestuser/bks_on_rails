- shift_ref = @shifts.count > 1 ? "shifts" : "shift"

%p
  We'd like to offer you the following extra #{shift_ref}:
%table
  %tr
    %th
      Time
    %th
      Restaurant
  - @shifts.each do |shift|
    %tr
      %td
        = shift.table_time
      %td
        = shift.restaurant.name

= render 'mailer/signature'
= render 'briefs'
= render 'reminders'



