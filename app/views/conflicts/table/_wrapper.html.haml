.table
  = render 'conflicts/table/row_header'
  / , caller: caller
  - conflicts.each do |conflict|
    = render 'conflicts/table/row', conflict: conflict
    / caller: caller, conflict: conflict, paths: paths